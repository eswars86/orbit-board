# OrbitBoard environment configuration
# Copy this file to `.env` before running:  cp .env.example .env

# Port the Express API listens on
PORT=3000

# Base URL the client uses to reach the API
API_URL=http://localhost:3000/api

# Path to the JSON data file (created/seeded at runtime)
DATA_FILE=./server/data/board.json
